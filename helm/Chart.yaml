apiVersion: v2
name: home-app
description: Home App - backend + frontend with optional PostgreSQL
type: application
version: 0.1.0
appVersion: "latest"

dependencies:
  - name: postgresql
    version: ~16.x
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: postgresql.enabled
