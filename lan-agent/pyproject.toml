[project]
name = "lan-agent"
version = "0.1.0"
description = "Reverse tunnel agent - connects to cloud gateway and forwards requests to local app"
requires-python = ">=3.11"
dependencies = [
    "websockets>=14.0",
    "httpx>=0.28.0",
]

[project.scripts]
lan-agent = "agent.main:main"
